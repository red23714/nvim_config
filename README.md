`.clang-format` в папку пользователя 
nvim в папку `.nvim/init.lua`

для vim требуется установка vimplug
```
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
    https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
```
потом `:PlugInstall`
конфиг в `~/.vimrc`
